# Порт приложения
PORT=3000

# URL Redis сервера
# Для локальной разработки (если Redis запущен локально)
# REDIS_URL=redis://localhost:6379

# Для Docker Compose (автоматически устанавливается в docker-compose.yml)
# REDIS_URL=redis://redis:6379
